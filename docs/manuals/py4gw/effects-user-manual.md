# Effects User Manual

`GW::effects` exposes selected gameplay effect controls.

Main API:

- `GetAlcoholLevel()`
- `GetDrunkAf(...)`
- `DropBuff(...)`

This subsystem is small and focused. Use it for effect-state inspection and buff dropping, not as a general effect database.
