# Guild LLM Manual

`guild` is a small action-oriented subsystem.

Keep its scope narrow:

- guild-hall travel
- player-guild convenience access

If broader guild state is needed, prefer context-level modeling in the guild context structures and keep this manager focused on behavior.
